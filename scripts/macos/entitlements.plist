<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN"
  "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- Allow loading unsigned libraries (required for PyInstaller bundles) -->
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>

    <!-- Allow DYLD environment variables (required for some Python C extensions) -->
    <key>com.apple.security.cs.allow-dyld-environment-variables</key>
    <true/>

    <!-- Disable Library Validation (required for bundled .so/.dylib files) -->
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>

    <!-- Network client access (for WeChat article fetching and AI API calls) -->
    <key>com.apple.security.network.client</key>
    <true/>

    <!-- File read/write for exports and config -->
    <key>com.apple.security.files.user-selected.read-write</key>
    <true/>
</dict>
</plist>
